# WI-010 Ranking Read Model Plan

## Implementation sequence

1. Bind the Stage-before-Ranking boundary, fixed key directions, no-goals, authorization, and current-WI issue policy in the Contract.
2. Add failing tests for key ordering, same-Stage isolation, deterministic ties, duplicate identity, and bounded values.
3. Implement pure Ranking value objects, explicit stages, candidates, and the stage-filtered Read Model.
4. Run focused tests, architecture checks, formatting, lint, and the full repository suite.
5. Record Summary and Outcome evidence, archive the WI, then publish and merge its PR.

## Scope guard

Stage inference, score calculation, evidence acquisition, persistence, report
generation, and delivery are deferred. If a problem remains within this
Contract, resolve it here instead of opening a new WI and expanding the graph.

## Verification commands

Use the commands listed in the design spec and Contract. The key result is that
the public API can rank one selected Stage without creating a cross-Stage total.

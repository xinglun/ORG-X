# WI-WR-011 Implementation Plan

1. Add the source-preserving semantic section parser with fenced-block protection.
2. Add typed limits, atomic chunk packing, boundary labels, and deterministic errors.
3. Register the interface module and add module-local, same-stem, and public integration tests.
4. Run focused tests, `make check`, strict AI Cockpit Finish, Archive, and local commit.

The plan deliberately excludes provider delivery, re-rendering, domain recalculation, persistence, scheduling, retry, and receipt behavior.
